snippet express-router
options head
  module.exports = (app) => {
    const router = require("express").Router();

    ${1:TARGET}

    return router;
  };

snippet angular-controller
options head
  module.exports = (app) => {
    app.controller("${TARGET}", [${2}, (${2}) => {
        ${1:TARGET}
    }]);
  };

